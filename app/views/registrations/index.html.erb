<h1>Registrations for <%= link_to @experience.title, @experience %></h1>
<ul class="registrations">
    <% @registrations.each do |r| %>
        <li>
            <%= pluralize(r.stars, 'star') %>
            <p>
                <%= r.name %>
                <span class="date">
                <%= time_ago_in_words(r.created_at) %> ago
                </span>
            </p>
        </li>
    <% end %>
</ul>